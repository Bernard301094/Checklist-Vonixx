// Sidebar removed — layout now uses Header-only top navbar.
// This file is kept as an empty stub to avoid breaking any residual imports.
export default function Sidebar(_props: { onLogout: () => void; role: 'supervisor' | 'colaborador' }) {
  return null;
}
